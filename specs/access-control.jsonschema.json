{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "AccessControlCloudEvent",
  "additionalProperties": true,
  "definitions": {
    "example.access-control.access.assign": {
      "required": ["data", "type"],
      "additionalProperties": true,
      "properties": {
        "type": {
          "type": "string",
          "enum": ["example.access-control.access.assign"]
        },
        "data": {
          "type": "object",
          "required": ["id", "userId", "role"],
          "properties": {
            "id": { "type": "string" },
            "userId": { "type": "string" },
            "role": { "$ref": "types/enums/access-role.jsonschema.json" },
            "expiresAt": {
              "$ref": "base_cloudevent_v1.0.1.json#/definitions/timedef"
            }
          }
        }
      }
    },
    "example.access-control.access.revoke": {
      "required": ["data", "type"],
      "additionalProperties": true,
      "properties": {
        "type": {
          "type": "string",
          "enum": ["example.access-control.access.revoke"]
        },
        "data": {
          "type": "object",
          "required": ["id"],
          "properties": {
            "id": { "type": "string" }
          }
        }
      }
    }
  },
  "allOf": [{ "$ref": "base_cloudevent_v1.0.1.json" }],
  "anyOf": [
    { "$ref": "#/definitions/example.access-control.access.assign" },
    { "$ref": "#/definitions/example.access-control.access.revoke" }
  ]
}
